#ifndef MAIN_H
#define MAIN_H

/* Standard library includes if needed strictly for size_t etc, though limits apply */
#include <stdlib.h>

int _putchar(char c);
void *malloc_checked(unsigned int b);

#endif /* MAIN_H */
